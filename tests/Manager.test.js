// const Manager = require('../lib/manager');
// const Employee = require('../lib/employee');

// describe("Manager", () => {
    // it("should should be an instance of manager class", () => {

    //   expect(new Employee()).toBeInstanceOf(Employee);
    // });

    // it("should create an object keys first name, last name, full name, id, email role", () => {
    //   const manager = new Manager.mockReturnValue("Sarah", "Jones", 5, "sjones@gmail.com");
    //   // manager.getRole();
    //   console.log('1', manager)

    //   expect(manager).toEqual({ firstName: "Sarah", lastName: "Jones", fullName: "", id: 5, email: "sjones@gmail.com", role: "Employee"});
    // });

    // it("should create an object with fullName", () => {
    //   const employee = new Employee("Sarah", "Jones");
    //   employee.getName();

    //   expect(employee.fullName).toEqual("Sarah Jones");
    // });

    // it("should create an object with role equal to employee", () => {
    //   const employee = new Employee("Sarah", "Jones");
    //   employee.getRole();

    //   expect(employee.role).toEqual("Employee");
    // });
    
  // });

